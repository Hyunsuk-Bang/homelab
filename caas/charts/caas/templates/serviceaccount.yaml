{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "caas.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "caas.labels" . | nindent 4 }}
{{- end }}
